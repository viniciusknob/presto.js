(function (window) {
  "use strict";

  const _Module = (function () {
    return {
      env: "__env__",
      models: {},
      modules: {},
      pages: {},
    };
  })();

  window.Presto = _Module;
})(window);
